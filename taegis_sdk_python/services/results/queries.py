"""Results Query."""

# pylint: disable=no-member, unused-argument, too-many-locals, duplicate-code, wildcard-import, unused-wildcard-import, cyclic-import


# Autogenerated
# DO NOT MODIFY

from __future__ import annotations

import logging
from typing import TYPE_CHECKING, Any, Dict, List, Optional, Tuple, Union

from taegis_sdk_python import GraphQLNoRowsInResultSetError
from taegis_sdk_python._consts import TaegisEnum
from taegis_sdk_python.services.results.types import *
from taegis_sdk_python.utils import (
    build_output_string,
    parse_union_result,
    prepare_input,
)

if TYPE_CHECKING:  # pragma: no cover
    from taegis_sdk_python.services.results import ResultsService

log = logging.getLogger(__name__)


class TaegisSDKResultsQuery:
    """Taegis Results Query operations."""

    def __init__(self, service: ResultsService):
        self.service = service

    def search_results(
        self, rn: str, cursor: Optional[str] = None, limit: Optional[int] = None
    ) -> SearchResultPage:
        """List results from a completed run."""
        endpoint = "searchResults"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={
                "rn": prepare_input(rn),
                "cursor": prepare_input(cursor),
                "limit": prepare_input(limit),
            },
            output=build_output_string(
                SearchResultPage,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return SearchResultPage.from_dict(result.get(endpoint))
        raise GraphQLNoRowsInResultSetError("for query searchResults")

    def search_results_totals(
        self, history_rns: Optional[List[str]] = None
    ) -> List[SearchResultTotalResponse]:
        """List results counts for a list of searches by query history RN (if succeeded)."""
        endpoint = "searchResultsTotals"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={
                "historyRNs": prepare_input(history_rns),
            },
            output=build_output_string(
                SearchResultTotalResponse,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return SearchResultTotalResponse.schema().load(
                [r or {} for r in result.get(endpoint)], many=True
            )
        raise GraphQLNoRowsInResultSetError("for query searchResultsTotals")

    def search_result_histogram(
        self, rn: str, bucket_width: HistogramDurationInput
    ) -> SearchResultHistogram:
        """Get the number of events bucketed within a time range."""
        endpoint = "searchResultHistogram"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={
                "rn": prepare_input(rn),
                "bucketWidth": prepare_input(bucket_width),
            },
            output=build_output_string(
                SearchResultHistogram,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return SearchResultHistogram.from_dict(result.get(endpoint))
        raise GraphQLNoRowsInResultSetError("for query searchResultHistogram")

    def search_result_facets(
        self,
        rn: str,
        fields: Optional[List[str]] = None,
        filter_: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
    ) -> SearchResultFacetPage:
        """Get values for the logical types of a query. If fields is null or empty, all
        available facets are returned."""
        endpoint = "searchResultFacets"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={
                "rn": prepare_input(rn),
                "fields": prepare_input(fields),
                "filter": prepare_input(filter_),
                "cursor": prepare_input(cursor),
                "limit": prepare_input(limit),
            },
            output=build_output_string(
                SearchResultFacetPage,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return SearchResultFacetPage.from_dict(result.get(endpoint))
        raise GraphQLNoRowsInResultSetError("for query searchResultFacets")
