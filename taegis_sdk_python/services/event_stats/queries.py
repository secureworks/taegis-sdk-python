"""EventStats Query."""

# pylint: disable=no-member, unused-argument, too-many-locals, duplicate-code, wildcard-import, unused-wildcard-import, cyclic-import


# Autogenerated
# DO NOT MODIFY

from __future__ import annotations

import logging
from typing import TYPE_CHECKING, Any, Dict, List, Optional, Tuple, Union

from taegis_sdk_python import GraphQLNoRowsInResultSetError
from taegis_sdk_python._consts import TaegisEnum
from taegis_sdk_python.services.event_stats.types import *
from taegis_sdk_python.utils import (
    build_output_string,
    parse_union_result,
    prepare_input,
)

if TYPE_CHECKING:  # pragma: no cover
    from taegis_sdk_python.services.event_stats import EventStatsService

log = logging.getLogger(__name__)


class TaegisSDKEventStatsQuery:
    """Taegis Event_stats Query operations."""

    def __init__(self, service: EventStatsService):
        self.service = service

    def effective_agents(self, args: EffectiveAgentsQuery) -> EffectiveAgentsResponse:
        """Get effective agents count for the given tenant and time range."""
        endpoint = "effectiveAgents"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={
                "args": prepare_input(args),
            },
            output=build_output_string(
                EffectiveAgentsResponse,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return EffectiveAgentsResponse.from_dict(result.get(endpoint))
        raise GraphQLNoRowsInResultSetError("for query effectiveAgents")
