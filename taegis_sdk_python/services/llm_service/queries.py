"""LlmService Query."""

# pylint: disable=no-member, unused-argument, too-many-locals, duplicate-code, wildcard-import, unused-wildcard-import, cyclic-import


# Autogenerated
# DO NOT MODIFY

from __future__ import annotations

import logging
from typing import TYPE_CHECKING, Any, Dict, List, Optional, Tuple, Union

from taegis_sdk_python import GraphQLNoRowsInResultSetError
from taegis_sdk_python._consts import TaegisEnum
from taegis_sdk_python.services.llm_service.types import *
from taegis_sdk_python.utils import (
    build_output_string,
    parse_union_result,
    prepare_input,
)

if TYPE_CHECKING:  # pragma: no cover
    from taegis_sdk_python.services.llm_service import LlmServiceService

log = logging.getLogger(__name__)


class TaegisSDKLlmServiceQuery:
    """Taegis Llm_service Query operations."""

    def __init__(self, service: LlmServiceService):
        self.service = service

    def nl_search_v2(self, in_: NLSearchInputsV2) -> NLSearchOutputV2:
        """nlSearch takes a natural language query input and converts to QL as the output. If many results are returned as output, the best result is first at the top of the list."""
        endpoint = "nlSearchV2"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={
                "in": prepare_input(in_),
            },
            output=build_output_string(
                NLSearchOutputV2,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return NLSearchOutputV2.from_dict(result.get(endpoint))
        raise GraphQLNoRowsInResultSetError("for query nlSearchV2")

    def example_prompt_catalog(self) -> ExamplePromptCatalog:
        """Categories and prompts; within each category, prompts with priority set first (ascending), then prompts with no priority in stable order."""
        endpoint = "examplePromptCatalog"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={},
            output=build_output_string(
                ExamplePromptCatalog,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return ExamplePromptCatalog.from_dict(result.get(endpoint))
        raise GraphQLNoRowsInResultSetError("for query examplePromptCatalog")

    def example_prompts(self) -> List[ExamplePrompt]:
        """Flat list: prompts with priority set first (ascending), then prompts with no priority in stable order."""
        endpoint = "examplePrompts"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={},
            output=build_output_string(
                ExamplePrompt,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return ExamplePrompt.schema().load(
                [r or {} for r in result.get(endpoint)], many=True
            )
        raise GraphQLNoRowsInResultSetError("for query examplePrompts")
