"""QueryRelay Query."""

# pylint: disable=no-member, unused-argument, too-many-locals, duplicate-code, wildcard-import, unused-wildcard-import, cyclic-import


# Autogenerated
# DO NOT MODIFY

from __future__ import annotations

import logging
from typing import TYPE_CHECKING, Any, Dict, List, Optional, Tuple, Union

from taegis_sdk_python import GraphQLNoRowsInResultSetError
from taegis_sdk_python._consts import TaegisEnum
from taegis_sdk_python.services.query_relay.types import *
from taegis_sdk_python.utils import (
    build_output_string,
    parse_union_result,
    prepare_input,
)

if TYPE_CHECKING:  # pragma: no cover
    from taegis_sdk_python.services.query_relay import QueryRelayService

log = logging.getLogger(__name__)


class TaegisSDKQueryRelayQuery:
    """Taegis Query_relay Query operations."""

    def __init__(self, service: QueryRelayService):
        self.service = service

    def fetch_query_relay_results(
        self, input_: FetchQueryRelayResultsInput
    ) -> QueryRelayResultsResponse:
        """Poll execution status and fetch paginated results for a submitted query.
        - Always returns current status, result state, error, and timestamps.
        - When status is finished and result is succeeded, also returns rows, metadata, and pages.
        - For subsequent pages of a completed query, pass pages.nextKey as nextPageToken.
        """
        endpoint = "fetchQueryRelayResults"

        result = self.service.execute_query(
            endpoint=endpoint,
            variables={
                "input": prepare_input(input_),
            },
            output=build_output_string(
                QueryRelayResultsResponse,
                exclude_deprecated_output=self.service.exclude_deprecated_output,
            ),
        )
        if result.get(endpoint) is not None:
            return QueryRelayResultsResponse.from_dict(result.get(endpoint))
        raise GraphQLNoRowsInResultSetError("for query fetchQueryRelayResults")
